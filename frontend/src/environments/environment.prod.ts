// Production (Vercel) — points at the deployed Spring Boot backend on Render.
export const environment = {
  production: true,
  apiBase: 'https://virtual-meeting-sentinel.onrender.com',
  wsUrl: 'https://virtual-meeting-sentinel.onrender.com/ws',
};
